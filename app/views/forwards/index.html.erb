<h1>Listing forwards</h1>

<table>
  <tr>
    <th>User</th>
    <th>Init user</th>
    <th>Message</th>
    <th>Comment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @forwards.each do |forward| %>
  <tr>
    <td><%= forward.user_id %></td>
    <td><%= forward.init_user_id %></td>
    <td><%= forward.message_id %></td>
    <td><%= forward.comment %></td>
    <td><%= link_to 'Show', forward %></td>
    <td><%= link_to 'Edit', edit_forward_path(forward) %></td>
    <td><%= link_to 'Destroy', forward, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Forward', new_forward_path %>
